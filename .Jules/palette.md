## 2026-05-26 - [Tooltip for Truncated Text]
**Learning:** The application truncates job task strings for visual simplicity but omitted `title` attributes, making the full context inaccessible.
**Action:** When truncating text for visual UI constraints, always include the full text in a `title` attribute on the element to ensure accessibility and usability.
